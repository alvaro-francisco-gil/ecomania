# Changelog

All notable changes to ecomania are documented here.

## Unreleased

- Agentic foundation: root + per-package `AGENTS.md`, architecture doc, ralph AFK loop,
  plan-lifecycle dirs, and the `.claude/skills/` set (process skills ported; domain skills
  stubbed pending models/structure).
