rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default deny until media paths (post images, etc.) are defined.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
